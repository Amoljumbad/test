spring:
  boot:
    admin:
      client:
        url: hrrp://localhost:1111/
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/ashokit_ecomm
    
management:
  endpoints:
    web:
      exposure:
        include: '*'
        
